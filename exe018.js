// for cria um loop que consiste em três expressões (inicialização, condição, iteração)

console.log('Vai Iniciar!')
for (let c = 1; c <= 10; c++) {
    console.log(`Conta ${c}`)
}
console.log('Terminou!')
